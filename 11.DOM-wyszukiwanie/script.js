// zad 1

const list = document.querySelectorAll(".list");

console.log(list);

// zad 2

const showTags = (tag) => {
    const tags = document.getElementsByTagName(tag);
    console.log(tags);
};
showTags("ul");

// zad 3

var zad3var = document.querySelector(#list);

console.log(zad3var);

// zad4

function showElements(e) {
    const elements = dokument.querySelectorAll(e);
    console.log(elements);
}
showElements(['li', 'ul', 'span', 'div.list span', 'div#spans span']);